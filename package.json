{
  "name": "metamask-game",
  "version": "1.0.0",
  "description": "MetaMask-enabled web game with token transfer functionality",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "pm2": "pm2 start ecosystem.config.js --env production",
    "pm2:restart": "pm2 restart metamask-game",
    "pm2:logs": "pm2 logs metamask-game"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^7.0.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
